/**
 * @format
 */

import 'react-native';
import React from 'react';
import App from '../App';
import {render} from '../src/utils/test-utils';

it('renders correctly', () => {
  render(<App />);
});
